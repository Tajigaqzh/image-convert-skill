interface:
  display_name: "Image Format Converter"
  short_description: "Convert images between WebP, PNG, JPEG, AVIF, TIFF, GIF, HEIF/HEIC, and JP2 formats with customizable quality settings"
defaults:
  parallel: cpu-1
  report: true
